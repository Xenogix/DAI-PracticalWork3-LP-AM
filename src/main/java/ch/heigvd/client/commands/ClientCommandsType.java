package ch.heigvd.client.commands;

import ch.heigvd.shared.abstractions.CommandType;

/**
 * All the possible client commands.
 */
public enum ClientCommandsType implements CommandType {
    JOIN,
    INPUT
}
